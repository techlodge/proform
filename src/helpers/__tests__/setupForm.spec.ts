import { describe, it } from "vitest";

describe("setupForm", () => {
  it("should setup", async () => {
    // const { setupForm } = await import("@/helpers/setupForm");
    // const result = setupForm();
    // expect(result).toBeDefined();
  });
});
